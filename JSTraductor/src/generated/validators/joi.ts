// AUTO-GENERATED by schema-translator
import J from "joi";

export const UserSchema = J.object({
  "id": J.string().uuid().required(),
  "name": J.string().required(),
  "email": J.string().email().required(),
  "roles": J.array().items(J.valid("admin", "user", "editor")).min(1).unique()
}).unknown(false);

export const ProfileSchema = J.object({
  "bio": J.string(),
  "website": J.string().uri()
}).unknown(false);

export const RootSchema = J.object({
  "User": UserSchema,
  "Profile": ProfileSchema
}).unknown(false);
